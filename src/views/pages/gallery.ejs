<!DOCTYPE html>
<html>
  <head>
    <title>Gallery</title>
  </head>

  <body>
    <header>
      <h1>Gallery</h1>
    </header>

    <main>
      <% if (data.status === 'OK') { %>
        <% var gifs = data.gifs %>
        <% var imgsInRow = 0 %>
        <% var maxImgsPerRow = 6 %>
        <table>
          <tr>
            <% gifs.forEach(function(gif) { %>
              <% if (imgsInRow >= maxImgsPerRow) { %>
                </tr><tr>
                <% imgsInRow = 0 %>
              <% } %>
              <td>
                <a href=<%= `https://giphy.com/gifs/${gif.id}` %> target="_blank">
                  <img src=<%= gif.url %> alt=<%= gif.id %>>
                </a>
              </td>
              <% imgsInRow++ %>
            <% }); %>
          </tr>
        </table>
      <% } else { %>
        <p>Error - <%= data.status %></p>
      <% } %>
    </main>
  </body>
</html>